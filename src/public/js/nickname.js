function getNickname() {
    //extract value from input
    const nickname = document.getElementById("nickname").value;
    //remove the prompt since it is no longer needed
    document.getElementById("nickname_cnt").remove()
    // TODO: load game.js
}